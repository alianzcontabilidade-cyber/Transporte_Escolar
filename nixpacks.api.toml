[phases.setup]
nixPkgs = ["nodejs_20", "chromium", "nss", "freetype", "harfbuzz"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build --workspace=apps/web && npm run build --workspace=packages/api && mkdir -p packages/api/public && cp -r apps/web/dist/* packages/api/public/"]

[variables]
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "true"

[start]
cmd = "cd packages/api && npx tsx src/migrate.ts && node dist/index.js"
