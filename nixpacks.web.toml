[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --workspace=apps/web"]

[phases.build]
cmds = ["npm run build --workspace=apps/web"]

[start]
cmd = "npx serve apps/web/dist -p $PORT"
