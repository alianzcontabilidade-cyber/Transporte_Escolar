{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd /app && npm install && npm run build --workspace=apps/web && npm run build --workspace=packages/api"
  },
  "deploy": {
    "startCommand": "npx drizzle-kit push && node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
