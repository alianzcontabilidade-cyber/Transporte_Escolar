{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && cd ../.. && npm install && npm run build --workspace=apps/web && cd packages/api && tsc"
  },
  "deploy": {
    "startCommand": "npx drizzle-kit push && node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
